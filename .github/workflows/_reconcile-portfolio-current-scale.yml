name: Reconcile Portfolio Current Scale

on:
  push:
    branches:
      - agent/host-integration-portfolio-authority-separation
    paths:
      - .github/workflows/_reconcile-portfolio-current-scale.yml
      - research/runtime/_reconcile_portfolio_current_scale.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  reconcile:
    if: github.repository == 'vessaxor-spec/The-ever-evolving-orchestration-' && github.ref == 'refs/heads/agent/host-integration-portfolio-authority-separation'
    runs-on: ubuntu-24.04
    steps:
      - name: Check out exact research branch
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: agent/host-integration-portfolio-authority-separation

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: '3.11.15'
          cache: pip
          cache-dependency-path: ci/requirements-ci.lock

      - name: Install pinned validation environment
        run: |
          python -m pip install --disable-pip-version-check --upgrade "pip==26.1.2"
          python -m pip install --disable-pip-version-check --require-hashes --no-deps -r ci/requirements-ci.lock
          python -m pip install --disable-pip-version-check --no-build-isolation --no-deps -e .

      - name: Apply exact current-scale reconciliation
        run: python research/runtime/_reconcile_portfolio_current_scale.py

      - name: Prove two-file scope and truth
        run: |
          python - <<'PY'
          import subprocess
          expected = {
              'docs/stewardship/progress-tracker.md',
              'tests/test_documentation_control_plane_truth.py',
          }
          actual = set(subprocess.check_output(['git', 'diff', '--name-only'], text=True).splitlines())
          if actual != expected:
              raise SystemExit(f'unexpected current-scale write scope: expected={sorted(expected)} actual={sorted(actual)}')
          PY
          git diff --check
          python -m pytest -q tests/test_documentation_control_plane_truth.py

      - name: Commit exact current-scale reconciliation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- docs/stewardship/progress-tracker.md tests/test_documentation_control_plane_truth.py
          git diff --cached --check
          git commit -m 'Advance current validated scale after portfolio reconciliation'
          git push origin HEAD:refs/heads/agent/host-integration-portfolio-authority-separation
