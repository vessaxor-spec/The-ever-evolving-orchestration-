# Provider-backed Documentation Replay Trigger

This file is an ephemeral control marker for the same-repository pull-request trigger used to execute the staged provider-backed `documentation` replay from trusted `main`.

It is not evidence, policy, routing authority, activation authority, or a candidate-state change. The trigger pull request must not be merged. The replay workflow checks out the pull request base revision rather than this branch.
