# Regulated Specialist Evidence Stability Qualification — 2026-08-16

## Decision

The prior 30-day elapsed-time pre-expansion gate is replaced by an executable stability qualification. The active seven-day source-resolution cadence remains mandatory continuous drift monitoring; qualification does not replace it.

Qualification does not create registry-expansion authority. A specific next risk-tier batch still requires explicit approval.

## Practical qualification evidence

The sandbox practical run exercised the exact six-card / seven-claim pilot shape using production-equivalent evidence validation semantics in an isolated filesystem. Five complete clean authority-resolution replays passed. Three normalized repeatability runs produced the same digest, `d2ea786046ad32671040de3a70191e8fa29199375782ab1f8ab47bf2202c3883`. A controlled authority-move path passed without changing the claim statement or verification ownership.

The governed mutation matrix killed 15 of 15 cases:

1. authority unavailable;
2. undeclared redirect host;
3. URL host not declared;
4. expired evidence;
5. excessive evidence lifetime;
6. tier downgrade;
7. independent verification removed;
8. preparer and verifier collapsed;
9. stale behavior weakened;
10. conflict behavior weakened;
11. pilot scope expanded;
12. unknown schema field;
13. non-HTTPS authority;
14. duplicate claim ID;
15. specialist-card blob mismatch.

The sandbox has no outbound network. Therefore the sandbox resolution path used deterministic controlled fixtures and cannot by itself prove current public-network authority availability.

## External network evidence

That missing network dimension is supplied by the GitHub-hosted Ubuntu 24.04 production-resolver observation from `ISO Resolution Probe` run #5, run ID `31961987236`, at head `633a8572b6417c3f61ff3baba67bfbb5534bf604`. The unchanged production evidence resolver resolved all seven declared authorities after the ISO endpoint repair.

## Codex placeholder topology

For the user-approved test-only model substitution, the qualification records:

- GPT-5.6 Terra / medium reasoning as the primary qualification-execution placeholder;
- GPT-5.6 Luna / medium reasoning as the repetitive replay/mutation placeholder;
- GPT-5.6 Sol / xhigh reasoning as the high-reasoning review placeholder.

This is single-provider placeholder metadata only. It does not claim that three distinct models actually executed inside the sandbox, does not satisfy provider-diverse verification, and does not modify canonical production routing.

## Gate disposition

- executable stability qualification: satisfied;
- 30-day elapsed-time wait: removed as a pre-expansion blocker;
- weekly seven-day authority resolution: retained for continuous drift detection;
- explicit next-batch approval: still required;
- registry expansion: not automatically authorized.
