version: 1.1
status: active
reviewed_at: 2026-08-11
policy: >-
  Runtime-specific task routes must not mutate the shared documentation worker's
  implementation ordering. high_volume_simple already declares its primary,
  fallback, alternatives, and verifier topology in routing policy, while the
  canonical documentation worker retains its own general-purpose model set.
  Future runtime-specific worker changes must be task-scoped rather than applied
  through an unconditional worker override.

workers: {}
worker_overrides: {}
